# Tenant: icta
# Copy relevant lines into api/.env on this fork/server.

COUNTY_CODE=icta
DB_NAME=ictadb

# Optional overrides (usually set in config/counties/icta.json instead):
# CERT_COUNTY_NAME=
# COUNTY_LOGO_PATH=assets/tenants/icta/logo.png
