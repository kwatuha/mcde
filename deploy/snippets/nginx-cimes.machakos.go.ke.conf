# System nginx: TLS termination + reverse proxy to MCmes stack (Docker nginx on host port 8084).
#
# Prerequisites:
#   1. DNS A record: cimes.machakos.go.ke -> 84.247.128.58
#   2. Machakos Docker stack running (compose nginx listens on 0.0.0.0:8084)
#   3. Ports 80/443 reachable from the internet (firewall)
#
# Install (on the MCmes server):
#   sudo bash deploy/install-cimes-nginx-on-server.sh
#   sudo certbot --nginx -d cimes.machakos.go.ke
#
# Or non-interactive:
#   sudo certbot --nginx -d cimes.machakos.go.ke --non-interactive --agree-tos -m YOUR_EMAIL --redirect
#
# IMPORTANT: Admin JWTs embed large privilege lists (~8–16KB). nginx 1.18 HTTP/2
# defaults (http2_max_field_size 4k) cause stream failures → browser Axios
# "Network Error" after login. If another vhost enables `listen 443 ssl http2`
# (e.g. Baringo), HTTP/2 applies to this site too — keep the raised limits below.

server {
    listen 80;
    listen [::]:80;
    server_name cimes.machakos.go.ke;

    client_max_body_size 100M;

    # Large Authorization headers (admin JWT)
    client_header_buffer_size 32k;
    large_client_header_buffers 8 64k;
    # Valid on nginx 1.18 when this server (or another on :443) enables http2:
    http2_max_field_size 32k;
    http2_max_header_size 64k;

    location ^~ /.well-known/acme-challenge/ {
        default_type "text/plain";
        root /var/www/html;
        allow all;
    }

    location / {
        proxy_pass http://127.0.0.1:8084;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_buffer_size 128k;
        proxy_buffers 4 256k;
        proxy_busy_buffers_size 256k;
        proxy_connect_timeout 60s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
    }
}

# After certificates exist, certbot typically adds a second server block for :443.
# Manual HTTPS block example (uncomment and fix paths after certbot certonly):
#
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name cimes.machakos.go.ke;
#
#     ssl_certificate     /etc/letsencrypt/live/cimes.machakos.go.ke/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/cimes.machakos.go.ke/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     client_max_body_size 100M;
#     client_header_buffer_size 32k;
#     large_client_header_buffers 8 64k;
#     http2_max_field_size 32k;
#     http2_max_header_size 64k;
#
#     location / {
#         proxy_pass http://127.0.0.1:8084;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_buffer_size 128k;
#         proxy_buffers 4 256k;
#         proxy_busy_buffers_size 256k;
#     }
# }
