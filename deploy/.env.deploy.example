# Copy to deploy/.env.deploy on the server (or repo root) and adjust.
# docker compose reads this when you pass: --env-file deploy/.env.deploy

MACHAKOS_PUBLIC_URL=https://monitoring.icskenya.co.ke
MACHAKOS_CITIZEN_PUBLIC_URL=https://monitoring.icskenya.co.ke/citizen
MACHAKOS_API_PROXY=http://host.docker.internal:3002

# Optional: passed through Compose for public-dashboard (maps)
# VITE_MAPS_API_KEY=
